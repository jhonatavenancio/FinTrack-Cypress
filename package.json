{
  "name": "fintrack",
  "private": true,
  "version": "0.1.0",
  "description": "FinTrack — E2E test scaffolding (Cypress). See server/ and client/ for the app itself.",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run"
  },
  "devDependencies": {
    "@faker-js/faker": "^9.9.0",
    "cypress": "^13.15.0",
    "dotenv": "^17.4.2"
  }
}
